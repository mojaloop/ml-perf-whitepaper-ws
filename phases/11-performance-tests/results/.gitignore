# This directory is meant to store test results
# By default, we track the directory structure but not actual test data

# Uncomment the lines below if you want to ignore test data:
# [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
# **/*.log
# **/*.json
# **/*.csv

# Currently tracking everything for initial setup