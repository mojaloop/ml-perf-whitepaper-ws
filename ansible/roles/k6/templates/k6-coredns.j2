data:
  Corefile: |
    .:53 {
        errors
        health {
          lameduck 5s
        }
        ready
        log . {
          class error
        }
        kubernetes cluster.local in-addr.arpa ip6.arpa {
          pods insecure
          fallthrough in-addr.arpa ip6.arpa
        }
        hosts {
{% for entry in dfsp_host_entries %}
          {{ entry.ip }} {{ entry.hostnames[0] }}
{% endfor %}
          {{ hostvars['sw1-monitoring']['private_ip'] }} promfana-prometheus.local
          fallthrough
        }
        prometheus :9153
        forward . /etc/resolv.conf
        cache 30
        loop
        reload
        loadbalance
    }
