---
- name: Pre-flight
  ansible.builtin.import_role:
    name: _common

- name: Run each onboarding collection in order
  ansible.builtin.include_tasks: run_collection.yml
  loop: "{{ dfsp_onboard_collections }}"
  loop_control:
    loop_var: coll
