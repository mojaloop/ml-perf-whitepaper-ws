data:
  Corefile: |
    .:53 {
        errors
        health {
          lameduck 5s
        }
        ready
        log . {
          class error
        }
        kubernetes cluster.local in-addr.arpa ip6.arpa {
          pods insecure
          fallthrough in-addr.arpa ip6.arpa
        }
        hosts {
          {{ switch_node_ip }} account-lookup-service.local quoting-service.local ml-api-adapter.local
{% for entry in dfsp_host_entries %}
          {{ entry.ip }} {{ entry.host }}
{% endfor %}
          fallthrough
        }
        prometheus :9153
        forward . /etc/resolv.conf
        cache 30
        loop
        reload
        loadbalance
    }
